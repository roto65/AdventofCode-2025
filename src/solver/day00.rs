#![allow(dead_code, unused_variables, unused_mut)]

use crate::solver::shared;

static DAY_NUM: u32 = 0;

pub fn task_a() {
    let file = shared::read_input(DAY_NUM, false);
    let mut result: u128 = 0;
    
    println!("Result for task A of day {}: {}", DAY_NUM, result); // RES: 
}

pub fn task_b() {
    let file = shared::read_input(DAY_NUM, false);
    let mut result: u128 = 0;

    println!("Result for task B of day {}: {}", DAY_NUM, result); // RES: 
}